Setup:

Change the ID's and Names of your roles in new.js to allow it to work with your server.

Change the parent ID's of the categories in -apply, -new and -done.

To use -report make a new channel and name it reports.

Change settings in the config file.

Dependancies 
npm install ms -S
npm install nodemon